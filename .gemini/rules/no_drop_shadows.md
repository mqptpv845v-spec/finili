# Rule: No Drop Shadows

Never use `shadow`, `shadow-*`, or `drop-shadow-*` CSS classes on any elements, cards, buttons, or hover states for Finali. Always keep all design elements 100% flat and sharp.
